# Техническое задание: Административная панель Savory AI

## 1. Общее описание

Разработать веб-интерфейс административной панели для SaaS-системы управления ресторанами Savory AI. Панель предназначена для системных администраторов с ролью `admin`.

### Технологический стек (рекомендуемый)
- **Frontend**: React/Next.js
- **UI Kit**: Ant Design / Shadcn/ui / Tailwind CSS
- **State**: React Query / Zustand
- **Auth**: JWT (токен из API `/auth/login`)

### Backend API
- **Base URL**: `/` (Go Fiber REST API)
- **Auth**: JWT Bearer token в заголовке `Authorization`
- **Существующий модуль**: `/admin/*` endpoints

---

## 2. Структура сущностей

### 2.1 Пользователи (Users)
```typescript
interface User {
  id: number
  name: string
  company: string
  email: string
  phone: string
  role: 'user' | 'admin'
  is_active: boolean
  created_at: string
  updated_at: string
}
```

### 2.2 Организации (Organizations)
```typescript
interface Organization {
  id: number
  name: string
  phone: string
  admin_id: number
  admin: User
  users: User[]
  languages: Language[]
  subscription: Subscription | null
  created_at: string
}
```

### 2.3 Рестораны (Restaurants)
```typescript
interface Restaurant {
  id: number
  organization_id: number
  name: string
  address: string
  phone: string
  website: string
  description: string
  image_url: string
  working_hours: WorkingHour[]
}

interface WorkingHour {
  id: number
  restaurant_id: number
  day_of_week: number  // 0 = Sunday, 1 = Monday, etc.
  open_time: string
  close_time: string
}
```

### 2.4 Подписки (Subscriptions)
```typescript
interface Subscription {
  id: number
  organization_id: number
  period: number          // месяцев
  start_date: string
  end_date: string
  is_active: boolean
}
```

### 2.5 Блюда (Dishes)
```typescript
interface Dish {
  id: number
  organization_id: number
  menu_category_id: number
  name: string
  price: number
  description: string
  image: string
  is_dish_of_day: boolean
  ingredients: Ingredient[]
  allergens: Allergen[]
}

interface Ingredient {
  id: number
  dish_id: number
  name: string
  quantity: number
}

interface Allergen {
  id: number
  dish_id: number
  name: string
  description: string
}
```

### 2.6 Столы (Tables)
```typescript
interface Table {
  id: number
  restaurant_id: number
  name: string
  guest_count: number
  menu: string
}
```

### 2.7 Категории меню (MenuCategory)
```typescript
interface MenuCategory {
  id: number
  name: string
  description: string
}
```

### 2.8 Языки (Languages)
```typescript
interface Language {
  id: number
  code: string
  name: string
  description: string
}
```

### 2.9 Лог действий админа (AdminLog)
```typescript
interface AdminLog {
  id: number
  admin_id: number
  admin: User
  action: 'create' | 'update' | 'delete' | 'block' | 'unblock' | 'activate' | 'deactivate' | 'view'
  entity_type: string     // user, organization, subscription, etc.
  entity_id: number
  details: string         // JSON
  ip_address: string
  created_at: string
}
```

---

## 3. Модули интерфейса

### 3.1 Dashboard (Главная)
**Путь**: `/admin/dashboard`

**Виджеты статистики**:
- Всего организаций / активных
- Всего пользователей / активных
- Активных подписок / истекающих в 7 дней
- Ресторанов всего
- Блюд в системе
- График регистраций за последние 30 дней
- Последние действия админов (5-10 записей)

---

### 3.2 Управление пользователями
**Путь**: `/admin/users`

**Функционал**:
| Действие | Описание |
|----------|----------|
| Список | Таблица с пагинацией, поиск по email/name/phone |
| Фильтры | По роли (user/admin), статусу (active/inactive) |
| Просмотр | Детальная карточка пользователя + его организации |
| Блокировка | Установка `is_active = false` |
| Изменение роли | user ↔ admin |
| Создание | Новый пользователь (только для суперадминов) |

**Колонки таблицы**:
- ID, Имя, Email, Телефон, Компания, Роль, Статус, Дата регистрации, Действия

---

### 3.3 Управление организациями
**Путь**: `/admin/organizations`

**Функционал**:
| Действие | Описание |
|----------|----------|
| Список | Таблица с пагинацией |
| Фильтры | По статусу подписки (активна/истекла/нет) |
| Просмотр | Организация + рестораны + пользователи + подписка |
| Редактирование | Изменение данных организации |
| Управление языками | Добавление/удаление языков организации |

**Карточка организации содержит**:
- Основная информация
- Список ресторанов (с возможностью перехода)
- Список пользователей организации
- Информация о подписке
- Статистика: кол-во ресторанов, столов, блюд

---

### 3.4 Управление подписками
**Путь**: `/admin/subscriptions`

**Функционал**:
| Действие | Описание |
|----------|----------|
| Список | Все подписки с фильтрами |
| Фильтры | Активные, истекшие, истекающие скоро |
| Создание | Назначить подписку организации |
| Продление | Увеличить период подписки |
| Деактивация | Принудительное отключение |

**Колонки**:
- ID, Организация, Период (мес), Дата начала, Дата окончания, Статус, Осталось дней, Действия

**Важно**:
- Выделять красным подписки, истекающие < 7 дней
- Выделять серым неактивные подписки

---

### 3.5 Рестораны
**Путь**: `/admin/restaurants`

**Функционал**:
- Список всех ресторанов системы
- Фильтр по организации
- Просмотр детальной информации
- Просмотр столов ресторана
- Просмотр меню (категории + блюда)

**Колонки**:
- ID, Название, Организация, Адрес, Телефон, Кол-во столов, Действия

---

### 3.6 Меню и блюда
**Путь**: `/admin/dishes`

**Функционал**:
- Список всех блюд с фильтрами
- Фильтры: организация, категория, блюдо дня
- Просмотр карточки блюда (ингредиенты, аллергены)
- Массовые операции (опционально)

**Колонки**:
- ID, Название, Категория, Организация, Цена, Блюдо дня, Действия

---

### 3.7 Логи действий
**Путь**: `/admin/logs`

**Функционал**:
- Таблица действий администраторов
- Фильтры: по админу, типу действия, типу сущности, дате
- Поиск по entity_id
- Экспорт в CSV (опционально)

**Колонки**:
- Дата/время, Админ, Действие, Тип сущности, ID сущности, IP, Детали

---

### 3.8 Языки системы
**Путь**: `/admin/languages`

**Функционал**:
- Список доступных языков
- Добавление нового языка
- Просмотр организаций, использующих язык

**Колонки**:
- ID, Код, Название, Описание, Кол-во организаций, Действия

---

## 4. Общие компоненты

### 4.1 Layout
```
┌─────────────────────────────────────────────────────────┐
│  Logo    [Search...]              [Admin Name] [Logout] │
├────────────┬────────────────────────────────────────────┤
│            │                                            │
│  Sidebar   │            Content Area                    │
│            │                                            │
│ Dashboard  │                                            │
│ Users      │                                            │
│ Orgs       │                                            │
│ Subscript  │                                            │
│ Restaurants│                                            │
│ Dishes     │                                            │
│ Logs       │                                            │
│ Languages  │                                            │
│            │                                            │
└────────────┴────────────────────────────────────────────┘
```

### 4.2 Общие UI компоненты
- **DataTable** - переиспользуемая таблица с пагинацией, сортировкой, фильтрами
- **SearchInput** - поиск с debounce
- **ConfirmModal** - подтверждение опасных действий
- **StatusBadge** - цветные бейджи статусов
- **DateRangePicker** - выбор периода для фильтров
- **ExportButton** - экспорт данных
- **Breadcrumbs** - навигация по разделам
- **EmptyState** - состояние пустого списка
- **LoadingSpinner** - индикатор загрузки
- **SkeletonLoader** - скелетон для контента

### 4.3 Уведомления
- Toast-уведомления для успешных действий
- Alert-модалы для ошибок
- Подтверждение перед удалением/блокировкой

---

## 5. API Endpoints

### 5.1 Аутентификация (существующие)
```
POST   /auth/login           - Вход в систему
GET    /auth/check           - Проверка токена
```

### 5.2 Dashboard
```
GET    /admin/stats          - Статистика для дашборда
```

**Response**:
```json
{
  "data": {
    "users": { "total": 100, "active": 85 },
    "organizations": { "total": 50, "active": 45 },
    "subscriptions": { "active": 40, "expiring_soon": 5 },
    "restaurants": { "total": 120 },
    "dishes": { "total": 1500 },
    "recent_logs": [...]
  }
}
```

### 5.3 Пользователи
```
GET    /admin/users                    - Список пользователей
GET    /admin/users/:id                - Детали пользователя
PATCH  /admin/users/:id                - Обновление пользователя
PATCH  /admin/users/:id/block          - Блокировка
PATCH  /admin/users/:id/unblock        - Разблокировка
PATCH  /admin/users/:id/role           - Изменение роли
POST   /admin/users                    - Создание пользователя
```

**Query параметры для списка**:
- `page` - номер страницы
- `limit` - элементов на странице
- `search` - поиск по email/name/phone
- `role` - фильтр по роли (user/admin)
- `is_active` - фильтр по статусу (true/false)
- `sort` - поле сортировки
- `order` - направление (asc/desc)

### 5.4 Организации
```
GET    /admin/organizations            - Список организаций
GET    /admin/organizations/:id        - Детали организации
PATCH  /admin/organizations/:id        - Обновление
GET    /admin/organizations/:id/restaurants  - Рестораны организации
GET    /admin/organizations/:id/users  - Пользователи организации
POST   /admin/organizations/:id/languages    - Добавить язык
DELETE /admin/organizations/:id/languages/:lang_id - Удалить язык
```

### 5.5 Подписки
```
GET    /admin/subscriptions            - Список подписок
GET    /admin/subscriptions/:id        - Детали подписки
POST   /admin/subscriptions            - Создание подписки
PATCH  /admin/subscriptions/:id        - Обновление
PATCH  /admin/subscriptions/:id/activate     - Активация
PATCH  /admin/subscriptions/:id/deactivate   - Деактивация
DELETE /admin/subscriptions/:id        - Удаление
```

**Query параметры**:
- `status` - active/expired/expiring_soon
- `organization_id` - фильтр по организации

### 5.6 Рестораны
```
GET    /admin/restaurants              - Список ресторанов
GET    /admin/restaurants/:id          - Детали ресторана
GET    /admin/restaurants/:id/tables   - Столы ресторана
GET    /admin/restaurants/:id/menu     - Меню ресторана
```

### 5.7 Блюда
```
GET    /admin/dishes                   - Список блюд
GET    /admin/dishes/:id               - Детали блюда
```

**Query параметры**:
- `organization_id` - фильтр по организации
- `category_id` - фильтр по категории
- `is_dish_of_day` - блюдо дня (true/false)

### 5.8 Логи
```
GET    /admin/logs                     - Список логов
GET    /admin/logs/export              - Экспорт в CSV
```

**Query параметры**:
- `admin_id` - фильтр по админу
- `action` - тип действия
- `entity_type` - тип сущности
- `entity_id` - ID сущности
- `date_from` - дата начала
- `date_to` - дата окончания

### 5.9 Языки
```
GET    /admin/languages                - Список языков
POST   /admin/languages                - Создание языка
GET    /admin/languages/:id            - Детали языка
PATCH  /admin/languages/:id            - Обновление
DELETE /admin/languages/:id            - Удаление
```

---

## 6. Безопасность

### 6.1 Авторизация
- Только пользователи с `role = admin` имеют доступ
- Проверка роли в middleware на всех `/admin/*` endpoints
- Refresh token механизм (опционально)

### 6.2 Логирование
- Все действия администраторов записываются в AdminLog
- Логируется: admin_id, action, entity_type, entity_id, details, ip_address

### 6.3 Защита
- Rate limiting на API
- CORS - только разрешённые домены
- Валидация всех входных данных
- SQL injection protection (GORM)
- XSS protection

---

## 7. UI/UX требования

### 7.1 Responsive Design
- Минимальная ширина: 1024px (desktop-first)
- Адаптивный sidebar (collapsible)
- Мобильная версия не обязательна (admin panel)

### 7.2 Состояния интерфейса
- **Loading**: спиннеры и skeleton loaders
- **Empty**: информативные пустые состояния
- **Error**: понятные сообщения об ошибках
- **Success**: toast-уведомления

### 7.3 Навигация
- Breadcrumbs на всех страницах
- Быстрый поиск в header
- Сохранение фильтров в URL

### 7.4 Таблицы
- Пагинация (10/25/50/100 элементов)
- Сортировка по колонкам
- Фильтры с сохранением в URL
- Выбор колонок для отображения (опционально)

---

## 8. Интернационализация (i18n)

### 8.1 Поддерживаемые языки
- Русский (ru) - основной
- Английский (en)

### 8.2 Локализация
- Интерфейс админ-панели
- Сообщения об ошибках
- Форматы дат и чисел

---

## 9. Приоритеты реализации

| Фаза | Модули | Описание | Приоритет |
|------|--------|----------|-----------|
| 1 | Auth, Layout | Авторизация, базовый layout | Критический |
| 2 | Dashboard | Дашборд со статистикой | Высокий |
| 3 | Users | Управление пользователями | Высокий |
| 4 | Organizations | Управление организациями | Высокий |
| 5 | Subscriptions | Управление подписками | Высокий |
| 6 | Restaurants | Просмотр ресторанов | Средний |
| 7 | Dishes | Просмотр блюд | Средний |
| 8 | Logs | Логи действий | Средний |
| 9 | Languages | Управление языками | Низкий |
| 10 | Export, i18n | Дополнительный функционал | Низкий |

---

## 10. Структура проекта (рекомендуемая)

```
admin-panel/
├── public/
├── src/
│   ├── api/                 # API клиент и endpoints
│   │   ├── client.ts
│   │   ├── auth.ts
│   │   ├── users.ts
│   │   ├── organizations.ts
│   │   └── ...
│   ├── components/          # Переиспользуемые компоненты
│   │   ├── ui/              # Базовые UI компоненты
│   │   ├── layout/          # Layout компоненты
│   │   └── features/        # Feature-компоненты
│   ├── hooks/               # Custom hooks
│   ├── pages/               # Страницы
│   │   ├── auth/
│   │   ├── dashboard/
│   │   ├── users/
│   │   ├── organizations/
│   │   └── ...
│   ├── stores/              # State management
│   ├── types/               # TypeScript типы
│   ├── utils/               # Утилиты
│   ├── i18n/                # Локализация
│   └── styles/              # Глобальные стили
├── package.json
└── README.md
```

---


### 12.1 Окружения
- Development
- Staging
- Production

 ---

### 12.2 CI/CD
- приложение должно запускаться локально через докер и прокидывать порт 3000 что бы можно было помотреть на него в браузере

---

## Приложение A: Wireframes

### A.1 Dashboard
```
┌────────────────────────────────────────────────────────────┐
│  [Users: 100]    [Orgs: 50]    [Subs: 40]    [Rest: 120]  │
├────────────────────────────────────────────────────────────┤
│                                                            │
│  ┌─────────────────────────┐  ┌─────────────────────────┐  │
│  │   Регистрации (график)  │  │   Последние действия    │  │
│  │                         │  │   • Admin1 создал user  │  │
│  │   📊                    │  │   • Admin2 обновил org  │  │
│  │                         │  │   • Admin1 блок user    │  │
│  └─────────────────────────┘  └─────────────────────────┘  │
│                                                            │
│  ┌─────────────────────────────────────────────────────┐   │
│  │   Истекающие подписки                               │   │
│  │   Org1 - 3 дня    Org2 - 5 дней    Org3 - 7 дней   │   │
│  └─────────────────────────────────────────────────────┘   │
└────────────────────────────────────────────────────────────┘
```

### A.2 Users List
```
┌────────────────────────────────────────────────────────────┐
│  Пользователи                              [+ Создать]     │
├────────────────────────────────────────────────────────────┤
│  [Поиск...]  [Роль: Все ▼]  [Статус: Все ▼]               │
├────────────────────────────────────────────────────────────┤
│  ID │ Имя      │ Email           │ Роль  │ Статус │ Дейст │
│  ───┼──────────┼─────────────────┼───────┼────────┼────── │
│  1  │ John     │ john@mail.com   │ admin │ ●      │ [...]  │
│  2  │ Jane     │ jane@mail.com   │ user  │ ●      │ [...]  │
│  3  │ Bob      │ bob@mail.com    │ user  │ ○      │ [...]  │
├────────────────────────────────────────────────────────────┤
│  [< Prev]  Страница 1 из 10  [Next >]    [10 ▼] на стр.   │
└────────────────────────────────────────────────────────────┘
```

---

## Приложение B: API Response форматы

### B.1 Успешный ответ
```json
{
  "data": { ... },
  "messages": ["success"],
  "code": 200
}
```

### B.2 Список с пагинацией
```json
{
  "data": {
    "items": [...],
    "pagination": {
      "page": 1,
      "limit": 10,
      "total": 100,
      "total_pages": 10
    }
  },
  "messages": ["success"],
  "code": 200
}
```

### B.3 Ошибка
```json
{
  "data": null,
  "messages": ["error message"],
  "code": 400
}
```
