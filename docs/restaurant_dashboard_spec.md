# Техническое задание: Личный кабинет ресторана (Restaurant Dashboard)

## 1. Общая информация

### 1.1 Название проекта
**Savory AI Restaurant Dashboard** — веб-приложение для управления рестораном с AI-ассистентом.

### 1.2 Цель проекта
Создание современного веб-интерфейса для владельцев и администраторов ресторанов, позволяющего управлять всеми аспектами работы заведения: меню, столиками, бронированиями, чатами с посетителями и аналитикой.

### 1.3 Целевая аудитория
- Владельцы ресторанов
- Администраторы заведений
- Менеджеры по работе с клиентами
- Хостес (управление бронированиями)

### 1.4 Технический стек (рекомендуемый)
- **Frontend**: React 18+ / Next.js 14+
- **UI Framework**: Tailwind CSS + shadcn/ui или Ant Design
- **State Management**: Zustand / TanStack Query
- **Charts**: Recharts / Chart.js
- **Real-time**: WebSocket для чатов
- **Язык**: TypeScript

---

## 2. Роли и права доступа

### 2.1 Роли пользователей

| Роль | Описание | Уровень доступа |
|------|----------|-----------------|
| **Admin** | Суперадминистратор системы | Полный доступ ко всем организациям |
| **Owner** | Владелец организации | Полный доступ к своей организации |
| **Manager** | Менеджер ресторана | Управление рестораном без финансов |
| **Staff** | Персонал | Только просмотр и работа с бронированиями |

### 2.2 Матрица доступа

| Функция | Admin | Owner | Manager | Staff |
|---------|-------|-------|---------|-------|
| Просмотр дашборда | ✅ | ✅ | ✅ | ✅ |
| Управление меню | ✅ | ✅ | ✅ | ❌ |
| Управление столами | ✅ | ✅ | ✅ | ❌ |
| Управление бронированиями | ✅ | ✅ | ✅ | ✅ |
| Просмотр чатов | ✅ | ✅ | ✅ | ✅ |
| Ответы в чатах | ✅ | ✅ | ✅ | ✅ |
| Управление сотрудниками | ✅ | ✅ | ❌ | ❌ |
| Настройки организации | ✅ | ✅ | ❌ | ❌ |
| Финансовая аналитика | ✅ | ✅ | ❌ | ❌ |
| Управление подпиской | ✅ | ✅ | ❌ | ❌ |
| Системное администрирование | ✅ | ❌ | ❌ | ❌ |

---

## 3. Структура приложения

### 3.1 Карта страниц

```
/login                          - Страница входа
/register                       - Регистрация
/forgot-password                - Восстановление пароля
/reset-password                 - Сброс пароля

/dashboard                      - Главная страница (обзор)
├── /restaurants                - Список ресторанов
│   ├── /restaurants/new        - Создание ресторана
│   └── /restaurants/:id        - Детали ресторана
│       ├── /edit               - Редактирование
│       └── /settings           - Настройки ресторана

├── /menu                       - Управление меню
│   ├── /menu/categories        - Категории меню
│   ├── /menu/dishes            - Список блюд
│   ├── /menu/dishes/new        - Добавление блюда
│   └── /menu/dishes/:id        - Редактирование блюда

├── /tables                     - Управление столами
│   ├── /tables/new             - Добавление стола
│   └── /tables/:id             - Редактирование стола

├── /reservations               - Бронирования
│   ├── /reservations/calendar  - Календарь бронирований
│   ├── /reservations/list      - Список бронирований
│   └── /reservations/:id       - Детали бронирования

├── /chats                      - Чаты с посетителями
│   ├── /chats/active           - Активные чаты
│   ├── /chats/history          - История чатов
│   └── /chats/:sessionId       - Конкретный чат

├── /questions                  - Быстрые вопросы
│   ├── /questions/menu         - Вопросы о меню
│   └── /questions/reservation  - Вопросы о бронировании

├── /qr-codes                   - QR-коды
│   └── /qr-codes/generate      - Генерация QR-кодов

├── /analytics                  - Аналитика
│   ├── /analytics/overview     - Общая статистика
│   ├── /analytics/reservations - Статистика бронирований
│   └── /analytics/chats        - Статистика чатов

├── /team                       - Команда
│   ├── /team/members           - Список сотрудников
│   └── /team/invite            - Приглашение сотрудника

├── /settings                   - Настройки
│   ├── /settings/profile       - Профиль пользователя
│   ├── /settings/organization  - Настройки организации
│   ├── /settings/languages     - Языки
│   └── /settings/subscription  - Подписка

└── /admin (только для Admin)   - Панель администратора
    ├── /admin/users            - Управление пользователями
    ├── /admin/organizations    - Управление организациями
    ├── /admin/moderation       - Модерация контента
    └── /admin/logs             - Логи действий
```

---

## 4. Детальное описание страниц

### 4.1 Аутентификация

#### 4.1.1 Страница входа `/login`

**Функциональность:**
- Форма входа с полями: email, пароль
- Кнопка "Войти"
- Ссылка "Забыли пароль?"
- Ссылка "Зарегистрироваться"
- Опция "Запомнить меня"

**Валидация:**
- Email: обязательное, формат email
- Пароль: обязательное, минимум 8 символов

**API:**
```
POST /auth/login
Body: { email, password }
Response: { token, user, organization }
```

**UI/UX требования:**
- Анимация загрузки при отправке формы
- Отображение ошибок валидации под полями
- Toast-уведомление при неверных данных
- Редирект на /dashboard после успешного входа

---

#### 4.1.2 Страница регистрации `/register`

**Функциональность:**
- Форма регистрации:
  - Название компании
  - Имя пользователя
  - Email
  - Телефон
  - Пароль
  - Подтверждение пароля
- Согласие с условиями использования

**Валидация:**
- Компания: обязательное
- Имя: обязательное
- Email: обязательное, уникальный, формат email
- Телефон: опционально, формат телефона
- Пароль: минимум 8 символов, буквы и цифры
- Подтверждение: должно совпадать с паролем

**API:**
```
POST /auth/register
Body: { company, name, email, phone, password }
Response: { id, email, name }
```

---

#### 4.1.3 Восстановление пароля `/forgot-password`

**Функциональность:**
- Поле ввода email
- Кнопка "Отправить код"
- Информационное сообщение об отправке

**API:**
```
POST /auth/request-password-reset
Body: { email }
```

---

#### 4.1.4 Сброс пароля `/reset-password`

**Функциональность:**
- Поле ввода кода из email
- Поле нового пароля
- Подтверждение пароля
- Кнопка "Сбросить пароль"

**API:**
```
POST /auth/verify-password-reset
Body: { email, code, newPassword }
```

---

### 4.2 Главный дашборд `/dashboard`

**Описание:** Обзорная страница с ключевыми метриками и быстрым доступом к основным функциям.

#### 4.2.1 Виджеты статистики (карточки)

| Виджет | Данные | Иконка |
|--------|--------|--------|
| Бронирования сегодня | Количество + динамика | 📅 |
| Активные чаты | Количество ожидающих ответа | 💬 |
| Столы | Занято / Всего | 🪑 |
| Блюда в меню | Общее количество | 🍽️ |

#### 4.2.2 График бронирований
- Линейный график за последние 7/30 дней
- Переключатель периода
- Сравнение с предыдущим периодом

#### 4.2.3 Список последних бронирований
- Таблица с 5 последними бронированиями
- Колонки: Время, Гость, Телефон, Столик, Статус
- Кнопка "Все бронирования"

#### 4.2.4 Лента активности чатов
- Последние 5 сообщений из активных чатов
- Превью сообщения, время, тип чата
- Кнопка "Перейти в чат"

#### 4.2.5 Быстрые действия
- Кнопка "Новое бронирование"
- Кнопка "Добавить блюдо"
- Кнопка "Скачать QR-код"

**API:**
```
GET /admin/stats (для Admin)
GET /reservations/restaurant/:id
GET /chat/restaurant/sessions/:id
```

---

### 4.3 Управление ресторанами `/restaurants`

#### 4.3.1 Список ресторанов

**Функциональность:**
- Карточки ресторанов с фото, названием, адресом
- Статус работы (открыт/закрыт по расписанию)
- Быстрые действия: Редактировать, QR-код, Удалить
- Кнопка "Добавить ресторан"

**API:**
```
GET /restaurants
DELETE /restaurants/:id
```

---

#### 4.3.2 Создание/редактирование ресторана

**Форма:**
```
Основная информация:
├── Название ресторана *
├── Адрес *
├── Телефон *
├── Веб-сайт
├── Описание (textarea)
└── Фото (загрузка изображения)

Настройки бронирования:
├── Длительность брони (30-480 минут) *
└── Минимальное время до брони

Расписание работы:
├── Понедельник: [открытие] - [закрытие] ☑️
├── Вторник: [открытие] - [закрытие] ☑️
├── ...
└── Воскресенье: [открытие] - [закрытие] ☑️
```

**Компонент расписания:**
- Переключатель "Выходной" для каждого дня
- Time picker для открытия/закрытия
- Кнопка "Копировать на все дни"

**API:**
```
POST /restaurants
PUT /restaurants/:id
POST /uploads/images (для фото)
```

---

### 4.4 Управление меню `/menu`

#### 4.4.1 Категории меню `/menu/categories`

**Функциональность:**
- Список категорий с возможностью drag-n-drop сортировки
- Количество блюд в каждой категории
- Создание/редактирование/удаление категорий
- Inline-редактирование названия

**UI компоненты:**
- Модальное окно создания категории
- Подтверждение удаления (если есть блюда)

**API:**
```
GET /categories
POST /categories
DELETE /categories/:id
```

---

#### 4.4.2 Список блюд `/menu/dishes`

**Функциональность:**
- Таблица или сетка блюд
- Фильтры: по категории, по цене, поиск по названию
- Сортировка: по названию, цене, дате добавления
- Массовые действия: удаление, перемещение в категорию
- Отметка "Блюдо дня" (звездочка)

**Колонки таблицы:**
| Фото | Название | Категория | Цена | Блюдо дня | Действия |
|------|----------|-----------|------|-----------|----------|

**API:**
```
GET /dishes
GET /dishes/category?categoryId=X
DELETE /dishes/:id
POST /dishes/dish-of-day/:id
```

---

#### 4.4.3 Создание/редактирование блюда `/menu/dishes/new`, `/menu/dishes/:id`

**Форма (многошаговая или табы):**

**Шаг 1: Основная информация**
```
├── Категория * (выпадающий список)
├── Название блюда *
├── Цена * (число)
├── Описание (textarea, макс 500 символов)
├── Фото блюда (drag-n-drop загрузка)
└── ☑️ Блюдо дня
```

**Шаг 2: Ингредиенты**
```
Динамический список:
├── [Название ингредиента] [Количество, г] [❌ Удалить]
├── [Название ингредиента] [Количество, г] [❌ Удалить]
└── [+ Добавить ингредиент]
```

**Шаг 3: Аллергены**
```
Динамический список:
├── [Название аллергена] [Описание] [❌ Удалить]
└── [+ Добавить аллерген]

Быстрый выбор (чекбоксы):
☐ Глютен  ☐ Молоко  ☐ Яйца  ☐ Орехи
☐ Рыба   ☐ Соя    ☐ Сульфиты
```

**Превью:**
- Предпросмотр карточки блюда как она будет выглядеть для гостя

**API:**
```
POST /dishes
PUT /dishes/:id
POST /uploads/images
```

---

### 4.5 Управление столами `/tables`

#### 4.5.1 Схема зала (визуальное представление)

**Функциональность:**
- Визуальная схема расположения столов
- Drag-n-drop позиционирование (опционально)
- Цветовая индикация статуса:
  - 🟢 Свободен
  - 🟡 Забронирован (скоро)
  - 🔴 Занят
- Клик по столу → детали и бронирования

**Альтернативный вид:**
- Табличный список столов

#### 4.5.2 Список столов

**Колонки:**
| Название | Вместимость | Текущий статус | Бронирований сегодня | Действия |

**API:**
```
GET /tables/restaurant/:restaurantId
```

---

#### 4.5.3 Создание/редактирование стола

**Форма:**
```
├── Название стола * (например: "Столик №5", "VIP-зона")
├── Вместимость * (количество гостей)
├── Ресторан * (выбор, если несколько)
└── Особое меню (опционально, текст)
```

**API:**
```
POST /tables
PUT /tables/:id
DELETE /tables/:id
```

---

### 4.6 Бронирования `/reservations`

#### 4.6.1 Календарь бронирований `/reservations/calendar`

**Функциональность:**
- Календарный вид (день/неделя/месяц)
- Таймлайн по столам (строки = столы, колонки = время)
- Цветовая маркировка статусов:
  - 🔵 Подтверждено
  - 🟡 Ожидает подтверждения
  - 🔴 Отменено
  - 🟢 Завершено
  - ⚫ Не явился
- Создание брони кликом на свободный слот
- Перетаскивание брони на другое время/стол

**Фильтры:**
- По ресторану
- По статусу
- По столу

---

#### 4.6.2 Список бронирований `/reservations/list`

**Таблица:**
| Дата | Время | Гость | Телефон | Гостей | Стол | Статус | Действия |

**Фильтры:**
- Дата (от-до)
- Статус
- Поиск по имени/телефону

**Действия:**
- Подтвердить
- Отменить
- Редактировать
- Удалить

**API:**
```
GET /reservations
GET /reservations/restaurant/:id
PATCH /reservations/:id
POST /reservations/:id/cancel
DELETE /reservations/:id
```

---

#### 4.6.3 Детали бронирования `/reservations/:id`

**Информация:**
```
Информация о госте:
├── Имя: Иван Петров
├── Телефон: +7 900 123-45-67
├── Email: ivan@example.com
└── Примечание: Столик у окна

Детали брони:
├── Дата: 15 декабря 2025
├── Время: 19:00 - 20:30
├── Гостей: 4
├── Стол: Столик №3
└── Статус: Подтверждено

История:
├── Создано: 10.12.2025 14:30 (через AI-чат)
├── Подтверждено: 10.12.2025 15:00 (Менеджер Анна)
└── ...
```

**Действия:**
- Изменить время/стол
- Изменить количество гостей
- Отменить бронь (с указанием причины)
- Отметить как "Завершено" / "Не явился"
- Связаться с гостем (открыть звонилку/мессенджер)

---

#### 4.6.4 Создание бронирования (модальное окно)

**Форма:**
```
Гость:
├── Имя *
├── Телефон *
└── Email

Бронирование:
├── Дата *
├── Время начала *
├── Количество гостей *
├── Стол * (автоподбор или ручной выбор)
└── Примечание

[Проверить доступность] [Создать бронь]
```

**Логика:**
1. После выбора даты/времени/гостей → запрос доступных столов
2. Автоматический подбор подходящего стола
3. Возможность ручного выбора из доступных

**API:**
```
GET /reservations/available/:restaurantId?date=X&guest_count=Y
POST /reservations
```

---

### 4.7 Чаты с посетителями `/chats`

#### 4.7.1 Интерфейс чатов (основной экран)

**Трёхколоночный layout:**

```
┌─────────────────┬──────────────────────────┬─────────────────┐
│  Список чатов   │     Окно чата            │  Инфо о госте   │
│                 │                          │                 │
│ 🔴 Столик №3    │  [Сообщения]             │  Столик №3      │
│    14:32        │                          │  Гостей: 4      │
│                 │  Гость: Какие у вас      │                 │
│ 🟢 Ресторан     │  есть пасты?             │  История:       │
│    14:28        │                          │  - Заказ №123   │
│                 │  AI: У нас есть          │  - Бронь 15.12  │
│ 🟢 Столик №7    │  Карбонара, Болоньезе... │                 │
│    14:15        │                          │  Быстрые        │
│                 │  [Поле ввода]            │  действия:      │
│ [Архив]         │                          │  [Создать бронь]│
└─────────────────┴──────────────────────────┴─────────────────┘
```

**Левая колонка - Список чатов:**
- Табы: "Активные" / "Архив"
- Фильтр по типу: Все / Столики / Ресторан
- Индикатор непрочитанных сообщений
- Превью последнего сообщения
- Время последнего сообщения
- Статус: ожидает ответа (красный), отвечено (зеленый)

**Центральная колонка - Окно чата:**
- История сообщений с аватарами
- Типы сообщений:
  - 👤 Гость (слева, светлый фон)
  - 🤖 AI-бот (справа, голубой фон)
  - 👨‍💼 Персонал (справа, зеленый фон)
- Время каждого сообщения
- Системные сообщения (бронь создана, сессия закрыта)
- Поле ввода с кнопкой отправки
- Кнопка "Закрыть чат"

**Правая колонка - Информация:**
- Информация о столике/ресторане
- История взаимодействий с гостем
- Быстрые действия:
  - Создать бронирование
  - Отправить меню
  - Вызвать официанта (для столика)

---

#### 4.7.2 Типы чатов

**Table Chat (Чат столика):**
- Инициируется гостем через QR-код на столе
- Контекст: конкретный столик и ресторан
- Гость может:
  - Задавать вопросы о меню
  - Вызвать официанта
  - Попросить счёт
- AI отвечает на вопросы о меню
- Персонал может вмешаться в диалог

**Restaurant Chat (Чат ресторана):**
- Инициируется через общий QR-код ресторана
- Гость может:
  - Узнать о меню
  - Забронировать столик
  - Задать общие вопросы
- AI может создавать бронирования (tool calls)
- Персонал видит все чаты и может отвечать

---

#### 4.7.3 Функция ответа персонала

**Когда персонал отвечает:**
1. Сообщение отмечается как "от персонала"
2. AI временно "замолкает" в этом чате
3. Персонал может:
   - Ответить текстом
   - Отправить быстрый ответ (шаблон)
   - Передать AI (кнопка "Вернуть AI")

**Шаблоны быстрых ответов:**
- "Сейчас подойдёт официант"
- "Ваш заказ готовится"
- "Столик забронирован, ждём вас!"
- (настраиваемые шаблоны)

---

#### 4.7.4 API для чатов

```
# Получение списка чатов
GET /chat/restaurant/sessions/:restaurantId
GET /chat/table/session/:tableId

# Получение сообщений
GET /chat/restaurant/session/:sessionId/messages
GET /chat/table/session/:sessionId/messages

# Отправка сообщения от персонала
POST /chat/restaurant/message/send
POST /chat/table/message/send
Body: { sessionId, content }

# Закрытие чата
POST /chat/restaurant/session/close/:sessionId
POST /chat/table/session/close/:sessionId
```

---

### 4.8 Быстрые вопросы `/questions`

**Описание:** Настройка предустановленных вопросов, которые гость видит в начале чата.

#### 4.8.1 Список вопросов

**Табы:** "Вопросы о меню" / "Вопросы о бронировании"

**Таблица:**
| Текст вопроса | Язык | Тип | Действия |

**Действия:**
- Редактировать
- Удалить
- Изменить порядок (drag-n-drop)

---

#### 4.8.2 Создание вопроса

**Форма:**
```
├── Текст вопроса * (например: "Какие у вас фирменные блюда?")
├── Тип * (меню / бронирование)
└── Язык (русский / английский / ...)
```

**API:**
```
GET /questions
GET /questions/language/:code
POST /questions
PUT /questions/:id
DELETE /questions/:id
```

---

### 4.9 QR-коды `/qr-codes`

#### 4.9.1 Генератор QR-кодов

**Функциональность:**

**QR-код ресторана:**
- Генерация общего QR-кода для ресторана
- При сканировании → общий чат с рестораном
- Опции:
  - Размер QR-кода
  - Формат (PNG/SVG/PDF)
  - С логотипом / без
- Предпросмотр
- Кнопки: "Скачать" / "Печать"

**QR-коды столиков:**
- Массовая генерация для всех столов
- Индивидуальная генерация по столу
- При сканировании → чат конкретного столика
- Опции оформления
- Генерация тейбл-тентов (шаблон для печати)

**API:**
```
GET /qrcodes/restaurant/:restaurantId
GET /qrcodes/restaurant/:restaurantId/download
GET /qrcodes/restaurant/:restaurantId/table/:tableId
GET /qrcodes/restaurant/:restaurantId/table/:tableId/download
```

---

### 4.10 Аналитика `/analytics`

#### 4.10.1 Общая статистика `/analytics/overview`

**Период:** Сегодня / Неделя / Месяц / Произвольный

**Карточки метрик:**
- Всего бронирований
- Подтверждённых
- Отменённых
- Не явились (no-show rate)
- Средняя заполняемость столов
- Активных чатов

**Графики:**
- Бронирования по дням (линейный)
- Распределение по времени (гистограмма)
- Популярность столов (pie chart)
- Загруженность по дням недели

---

#### 4.10.2 Статистика бронирований `/analytics/reservations`

**Метрики:**
- Конверсия: чат → бронирование
- Среднее количество гостей
- Пиковые часы
- Самые популярные столы
- Причины отмен

**Таблица топ-гостей:**
| Гость | Визитов | Последний визит | Средний чек |

---

#### 4.10.3 Статистика чатов `/analytics/chats`

**Метрики:**
- Всего сессий
- Среднее время ответа
- Сообщений на сессию
- % AI vs Персонал
- Популярные вопросы

---

### 4.11 Команда `/team`

#### 4.11.1 Список сотрудников

**Таблица:**
| Имя | Email | Роль | Статус | Добавлен | Действия |

**Действия:**
- Изменить роль
- Деактивировать
- Удалить

**API:**
```
GET /organization/:id (с users)
POST /organization/:id/users
DELETE /organization/:id/users
```

---

#### 4.11.2 Приглашение сотрудника

**Форма:**
```
├── Email *
├── Имя
├── Роль * (Manager / Staff)
└── [Отправить приглашение]
```

**Логика:**
1. Создаётся пользователь
2. Добавляется в организацию
3. Отправляется email со ссылкой для активации

---

### 4.12 Настройки `/settings`

#### 4.12.1 Профиль пользователя `/settings/profile`

**Форма:**
```
Личные данные:
├── Имя
├── Email (только чтение)
├── Телефон
└── Фото профиля

Безопасность:
├── [Сменить пароль]
└── Текущий пароль / Новый пароль / Подтверждение
```

**API:**
```
GET /user/:id
PATCH /user/:id
POST /auth/change-password
```

---

#### 4.12.2 Настройки организации `/settings/organization`

**Форма:**
```
├── Название организации
├── Контактный телефон
├── Логотип
└── Часовой пояс
```

**API:**
```
GET /organization/:id
PATCH /organization/:id
```

---

#### 4.12.3 Языки `/settings/languages`

**Функциональность:**
- Список доступных языков
- Выбор языков для организации
- Создание новых языков (для Admin)

**API:**
```
GET /languages
GET /organization/:id/languages
POST /organization/:id/languages
DELETE /organization/:id/languages
```

---

#### 4.12.4 Подписка `/settings/subscription`

**Информация:**
```
Текущий план:
├── Статус: Активна
├── Период: 12 месяцев
├── Начало: 01.01.2025
├── Окончание: 31.12.2025
├── Осталось дней: 365

[Продлить подписку]
```

**API:**
```
GET /subscriptions/organization/:id/active
POST /subscriptions/:id/extend
```

---

### 4.13 Панель администратора `/admin`

> Доступна только пользователям с ролью Admin

#### 4.13.1 Статистика системы `/admin`

**Карточки:**
- Всего пользователей / Активных
- Организаций
- Ресторанов
- Блюд в системе
- Активных подписок

**Лента активности:**
- Последние действия администраторов

**API:**
```
GET /admin/stats
```

---

#### 4.13.2 Управление пользователями `/admin/users`

**Таблица с пагинацией:**
| ID | Имя | Email | Компания | Роль | Статус | Создан | Действия |

**Действия:**
- Просмотреть детали
- Активировать/Деактивировать
- Изменить роль (user ↔ admin)
- Удалить

**API:**
```
GET /admin/users
GET /admin/users/:id
PATCH /admin/users/:id/status
PATCH /admin/users/:id/role
DELETE /admin/users/:id
```

---

#### 4.13.3 Управление организациями `/admin/organizations`

**Таблица:**
| ID | Название | Админ | Рестораны | Пользователи | Подписка | Действия |

**Действия:**
- Просмотреть детали
- Удалить

**API:**
```
GET /admin/organizations
GET /admin/organizations/:id
DELETE /admin/organizations/:id
```

---

#### 4.13.4 Модерация контента `/admin/moderation`

**Функциональность:**
- Просмотр всех блюд в системе
- Фильтрация по организации
- Удаление неподобающего контента

**API:**
```
GET /admin/dishes
DELETE /admin/dishes/:id
```

---

#### 4.13.5 Логи действий `/admin/logs`

**Таблица:**
| Дата | Админ | Действие | Объект | Детали | IP |

**Фильтры:**
- По администратору
- По типу действия
- По дате

**API:**
```
GET /admin/logs
GET /admin/logs/me
```

---

## 5. Компоненты UI

### 5.1 Общие компоненты

| Компонент | Описание |
|-----------|----------|
| `Sidebar` | Боковое меню навигации |
| `Header` | Шапка с поиском и профилем |
| `Breadcrumbs` | Хлебные крошки |
| `DataTable` | Таблица с сортировкой, фильтрами, пагинацией |
| `Modal` | Модальное окно |
| `Drawer` | Выдвижная панель |
| `Toast` | Уведомления |
| `Skeleton` | Скелетон загрузки |
| `EmptyState` | Пустое состояние |
| `ConfirmDialog` | Диалог подтверждения |

### 5.2 Специфичные компоненты

| Компонент | Описание |
|-----------|----------|
| `StatCard` | Карточка статистики |
| `ReservationCalendar` | Календарь бронирований |
| `TableFloorPlan` | Схема зала |
| `ChatWindow` | Окно чата |
| `ChatList` | Список чатов |
| `DishCard` | Карточка блюда |
| `QRCodePreview` | Превью QR-кода |
| `TimeSlotPicker` | Выбор времени |
| `WorkingHoursEditor` | Редактор расписания |

---

## 6. Требования к UI/UX

### 6.1 Адаптивность

- **Desktop** (1920px+): Полный интерфейс
- **Laptop** (1366px): Компактный sidebar
- **Tablet** (768px): Коллапсируемый sidebar
- **Mobile** (375px): Мобильная версия с бургер-меню

### 6.2 Темы

- Светлая тема (по умолчанию)
- Тёмная тема
- Системная (авто)

### 6.3 Доступность (a11y)

- Контраст текста ≥ 4.5:1
- Фокус-состояния для клавиатурной навигации
- ARIA-атрибуты для скринридеров
- Alt-тексты для изображений

### 6.4 Производительность

- Время первой загрузки < 3 сек
- Time to Interactive < 5 сек
- Ленивая загрузка изображений
- Виртуализация длинных списков
- Кэширование API-запросов

### 6.5 Локализация

- Поддержка русского и английского языков
- Форматирование дат по локали
- Форматирование валюты

---

## 7. Интеграция с API

### 7.1 Базовая конфигурация

```typescript
const API_BASE_URL = process.env.NEXT_PUBLIC_API_URL || 'http://localhost:4000';

const apiClient = axios.create({
  baseURL: API_BASE_URL,
  headers: {
    'Content-Type': 'application/json',
  },
});

// Interceptor для JWT
apiClient.interceptors.request.use((config) => {
  const token = localStorage.getItem('token');
  if (token) {
    config.headers.Authorization = `Bearer ${token}`;
  }
  return config;
});
```

### 7.2 Обработка ответов

```typescript
interface ApiResponse<T> {
  data: T;
  messages: string[];
  code: number;
}
```

### 7.3 Обработка ошибок

| Код | Описание | Действие |
|-----|----------|----------|
| 400 | Ошибка валидации | Показать ошибки полей |
| 401 | Не авторизован | Редирект на /login |
| 403 | Нет доступа | Показать сообщение |
| 404 | Не найдено | Страница 404 |
| 500 | Ошибка сервера | Показать уведомление |

---

## 8. Безопасность

### 8.1 Аутентификация
- JWT токены в localStorage
- Автоматический refresh (если реализован)
- Выход при истечении токена

### 8.2 Авторизация
- Проверка роли на клиенте (UI)
- Проверка роли на сервере (обязательно)
- Скрытие недоступных элементов

### 8.3 Защита данных
- HTTPS обязателен
- Санитизация пользовательского ввода
- Защита от XSS
- CSRF-токены (если используются cookies)

---

## 9. Тестирование

### 9.1 Unit-тесты
- Компоненты: React Testing Library
- Утилиты: Jest
- Покрытие ≥ 70%

## 10. Деплой и DevOps

### 10.1 Окружения
- **Development**: localhost
- **Staging**: staging.savory-dashboard.com
- **Production**: app.savory-dashboard.com

---

## 11. Этапы разработки

### Этап 1: MVP (4-6 недель)
- [ ] Аутентификация (login, register, logout)
- [ ] Базовый дашборд
- [ ] Управление рестораном (CRUD)
- [ ] Управление меню (категории + блюда)
- [ ] Управление столами

### Этап 2: Бронирования (2-3 недели)
- [ ] Список бронирований
- [ ] Календарь бронирований
- [ ] Создание/редактирование/отмена
- [ ] Получение доступных слотов

### Этап 3: Чаты (2-3 недели)
- [ ] Интерфейс чатов
- [ ] Список активных чатов
- [ ] История чатов
- [ ] Ответы от персонала

### Этап 4: Дополнительный функционал (2-3 недели)
- [ ] QR-коды генерация
- [ ] Быстрые вопросы
- [ ] Аналитика
- [ ] Управление командой

### Этап 5: Админ-панель (1-2 недели)
- [ ] Статистика системы
- [ ] Управление пользователями
- [ ] Модерация
- [ ] Логи

### Этап 6: Полировка (1-2 недели)
- [ ] Темная тема
- [ ] Мобильная адаптация
- [ ] Локализация
- [ ] Производительность
- [ ] Тестирование

---

## 12. Приложения

### 12.1 Макеты (Figma)
> Ссылка на макеты будет добавлена

### 12.2 API Документация
> См. файл `swagger.yaml`

---

## 13. Контакты

**Разработка API:** Savory AI Backend Team
**Документация:** docs/CLAUDE.md
**Тестовый сервер:** http://localhost:4000

---

*Документ создан: Ноябрь 2025*
*Версия: 1.0*
